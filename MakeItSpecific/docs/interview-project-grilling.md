# 项目拷打面试流程 — Alfred (AI Agent)

> 面向「后端 / Agent 工程师」岗位的**项目深挖**面试题集。按真实面试节奏分 6 个阶段。
> 难度：⭐ 送分题 / ⭐⭐ 常规深挖 / ⭐⭐⭐ 追命题（考你是否真读过代码、能否顶住连环追问）。
> **本文件只出题、不写答案。** 你自己写答案，再回来逐题过。

---

## 阶段 0：开场 & 自我介绍（2–3 分钟）

0.1 用 2 分钟介绍这个项目。面试官只听，不打断。（⭐⭐）

0.2 一句话说清：Alfred 解决什么问题？用户是谁？和 ChatGPT 直接对话有什么区别？

0.3 你在项目里独立负责的部分和最有成就感的一个点。

---

## 阶段 1：项目概述（Overview）

1.1 这个项目为什么从 Portal 的 MakeItSpecific 子项目独立出来？独立带来了哪些结构变化？

1.2 技术选型：为什么是 FastAPI + LangGraph + LangChain + PGVector + DashScope？如果今天重选，你会换掉哪个？

我会选择换掉langchain部分，那是初学的时候了解使用大模型，现在会尝试仿照最新的架构尝试写一个翻版。

> 📝 **点评**：方向对、也够诚实（"初学时候用的"），但"仿照最新架构写翻版"太虚。面试官一定会追一句"**LangChain 具体哪里不好？**"——你得说出具体痛点：抽象层过厚（Runnable/LCEL 把细节藏太深、报错难定位）、版本 churn 大、你其实只用到了其中一小部分。还要能自圆其说"换成什么、为什么"——注意 `create_react_agent` 和 `@tool` 本身就来自 LangChain/LangGraph 生态，说"换掉 LangChain"会被反问"那 ReAct 循环你自己手写吗"。

1.3 一条用户消息从进来到输出，中间经历了哪些节点？用一张图画出来。

信息a - 读入 - 意图识别 - enrich query - rag注入信息 - planner判断信息是否充足 - 重新问询要求补充信息 或 执行输出

> 📝 **点评**：**只答到 planner 就断了**，后半段最关键的执行+质检链路全漏了。完整是：`router → enrich → rag → planner → {clarify | engineering_check} → {execute | multi_agent} → checkpoint → {reflect | execute}`。尤其漏了 `engineering_check`（工程规范三级）、`multi_agent`（三立场）、`checkpoint`/`reflect`（双层质检）。面试官会据此判断你对整条图不熟。

---

## 阶段 2：架构与核心设计

2.1 为什么用 LangGraph 而不是手写一个 while 循环？图给你的关键能力是什么？

循环像是一个闭环的图，路径单一。但是使用图的话，第一能有效的根据意图区分到不同的路径，耦合性更低；第二图自由度更广，节点直接交互能力更强；第三图实现了该agent反问的更强效的能力，能在节点直接往复且时效性更强

> 📝 **点评**：沾边但没答到核心。图相对 while 循环的关键能力是：① **显式状态机**（`AgentState` 跨节点共享，可注入、可检查、可恢复）；② **条件路由**（`add_conditional_edges` 让"下一步去哪"由函数决定，不用手写 if 分支）；③ **循环+重试**（`checkpoint→execute`、`reflect→execute` 的往复，带次数上限）。你说的"耦合更低"其实不准——图节点之间靠共享 state 是**强耦合**的；"时效性更强"表述不清。

2.2 `AgentState` 里挂了哪些字段？哪些是跨节点共享的状态，哪些是单节点临时值？（⭐⭐）

？？？

2.3 为什么上下文要分 L1 / L2 / L3 三层？如果只用一层"最近 10 轮原文"会出什么问题？

L1是原文，L2是动态窗口维护的摘要，L3是事实记忆。这样设计有几点好处，1节省token、2逐层往上，关键信息更加精准，主动舍去冗余、矛盾信息，3区分不同信息的重要程度，避免上下文过程淹没了关键信息。如果只用原文，上下文过长、信息捕捉不全，浪费模型上下文能力

> 📝 **点评**：主体对，但有一处**硬伤**：L1 才是"窗口"（最近 3 轮原文），L2 才是"滚动摘要"——你说反了（"L2 是动态窗口维护的摘要"）。另外漏了最核心的动机：**防遗忘 + 防幻觉**（L3 语义事实按需召回，避免长对话把关键约束丢了、或模型凭印象编）。"主动舍去矛盾信息"这点你项目里其实**没有**去重/去矛盾逻辑，别自己加戏。

2.4 你引入"多 Agent Panel（三立场）"节点是干嘛的？它和 Execute 里的 ReAct agent 是并列还是包含关系？

三立场agent是在输出的时候通过温度调整，让输出兼顾稳重、实用、创新三个点。和ReAct agent是包含关系，后者是他们的指挥

> 📝 **点评**：**两处都错，而且错在根本机制上。** ① 三立场不是"温度调整"，是三个**不同的 System Prompt**（实用派/稳健派/创新派，见 `services/multi_agent.py`），用 `asyncio.gather` **并行**跑，最后第 4 个 LLM（阿福 synthesizer）整合成结构化对比。② 不是"包含关系、ReAct 指挥它们"——`multi_agent` 和 `execute` 是**并列节点**，都从 `engineering_check` 分出来、都汇到 `checkpoint`（`graph.py:980-985`），谁也不是谁的"指挥"。

---

## 阶段 3：项目拷打（核心 · 最细 · 最难）

> 面试官会从这里开始"钻进去"，每问完一个就顺着你的回答继续往深追。以下按模块分组。

### 3.1 上下文引擎（ContextEngine）

3.1.1 你的 L3 语义事实走"双路径持久化"——主路径 PGVector、后备内存字典。**内存字典真的算"持久化"吗？它什么时候会丢？**（⭐⭐）

并不持久，在下次写入的时候就会被覆盖

> 📝 **点评**：方向对（确实不持久），但**丢失时机答错了**。内存字典不是"下次写入被覆盖"，而是：① **进程重启就整个丢**（这才是"不持久"的本质）；② 运行时靠 **LRU 驱逐**——每个 session 最多 50 条事实、全局最多 500 个 session（`context_engine.py:449-454`）。所以它是"易失缓存"，真正持久的是 PGVector 那一路。

3.1.2 L3 事实在 PGVector 里以什么粒度存储？为什么"一条事实 = 一行"是后来改的？改之前是什么粒度、有什么问题？（⭐⭐⭐）

？？？

3.1.3 如果 embedding 服务整个挂了，L3 事实的提取和召回分别会怎样？降级链走通吗？（⭐⭐）

提取会直接挂掉。召回会大打折扣，降级链走一开始计算好的向量cosine分数

> 📝 **点评**：**两处都要修正。** ① 提取侧：LLM 提取失败会**降级到正则规则提取**（`_extract_atomic_facts`，`context_engine.py:358-361`），不是"直接挂掉"；② 召回侧：embedding 挂了**根本算不出向量**，所以不存在"走预先算好的 cosine"——实际是降级到**内存字典关键词匹配**（`_retrieve_facts_memory`）。你说的"预先算好的向量 cosine"代码里没有。

3.1.4 话题切换检测你说是"keyword 重叠 + LLM 精检"。代码里重叠率落在中间地带（0 < overlap < 0.3）时，**真的调了 LLM 精检吗？** 还是注释写了但没实现？（⭐⭐⭐）

？？？

3.1.5 内存字典的 LRU 驱逐阈值是多少？驱逐逻辑本身有没有 bug？你是什么时候发现的？（⭐⭐⭐）

记得是500；目前的驱逐逻辑是有bug的，只有权重增加没有减少；在我查看内存字典代码实现的时候

> 📝 **点评**：阈值 500 对。但 bug 描述错了——不是"权重只增不减"，是 `_evict_old_sessions_locked` 末尾残留了一行 `return "\n".join(lines)`，`lines` 变量不存在，session 数超阈值驱逐完就抛 `NameError`。

3.1.6 L3 检索按 `session_id` 过滤，但你某处注释写"跨会话可用"。**这两者矛盾吗？** 到底是跨会话还是会话隔离？（⭐⭐）

？？？

3.1.7 L2 滚动摘要的增量更新是怎么做的？为什么不每次重建全量历史？（⭐⭐）

是从第四次对话开始，对历史摘要+此轮对话进行摘要更新（第四轮对前面历史信息统一摘要）。每次重建全量历史上下文太广，这样做不容易丢失关键信息且节省token。

> 📝 **点评**：方向对（增量不重建、省 token）。但"第四次开始"不准——是**每轮后都增量更新**，只是第一轮跳过（`turn_count <= 1` 早退）。没有"第四次"这个特殊起点。

### 3.2 RAG 检索

3.2.1 dense 检索的 embedding 作用在什么文本上？sparse 作用在什么文本上？**为什么两段文本不一样？** 各自想抓什么？（⭐⭐⭐）

稠密检索在文本正文信息。稀疏检索一些专有名词，例如名字-文章名-技术名词等。。。。

> 📝 **点评**：方向对，但不够代码级。dense 作用在 `search_text_dense` = **title + category + 实体名 + section_title + body**（不只有正文）；sparse 作用在 `search_text_sparse` = **repository + url + author + category + 实体名**。一个抓语义、一个抓词面精确。

3.2.2 RRF 融合的原理用一句话 + 公式说清。**为什么用 RRF 而不是把 cosine 和 ts_rank 归一化后加权求和？**（⭐⭐⭐）

置信度/（k+rank_k）；不知道

> 📝 **点评**：公式对（`Σ 1/(k+rank)`，k=60）。但"为什么不用归一化加权"是这题的**关键分**——因为 cosine 和 ts_rank 尺度不同，加权要先归一化、对异常值敏感；RRF 只看排名、天然免归一化。

3.2.3 rerank（qwen3-rerank）和 embedding（双塔）在原理上有什么区别？为什么 rerank 只对粗排 top-20 做、而不是对全库做？（⭐⭐⭐）

rerank是把rag信息和文本信息编排后发给模型再rank打分，embedding只是算向量cosine和ts-rank。前者慢但精准，后者快但粗糙，对top-20是一次平衡性能和时间

> 📝 **点评**：✅ 答到点上了。补一句更精确：embedding 是双塔（query/doc 各自编码再算余弦，无 token 交互），rerank 是交叉编码器（query+doc 拼一起过 transformer，token 直接交互）。这题你过了。

3.2.4 你的相似度过滤是 `score >= 0.6 OR rerank_score >= 0.3`。这里的 `score` 是什么尺度？**一个只被 sparse 路召回、没被 dense 路召回的文档，它的 `score` 是 cosine 还是 ts_rank？能过 0.6 的门吗？**（⭐⭐⭐）

我也想问，这里打分具体是怎么实现的。

> 📝 **点评**：见 `interview-answer-key.md` 3.2.4。一句话：RRF 融合后 `score` 保留**第一次出现那路**的尺度——dense 在前，所以两路都召回的是 cosine；**只被 sparse 召回的 `score` 是 ts_rank**，过不了 0.6 门，只能靠 `rerank_score≥0.3` 捞回。

3.2.5 你说稀疏检索是"BM25"，实际用什么实现的？**GIN 索引建在哪个字段？稀疏检索查的是哪个字段？这个索引对稀疏检索生效吗？**（⭐⭐⭐）

实际使用的pgvector里面的ts-rank。后面的不知道

> 📝 **点评**：前半对（ts_rank）。后半是这题考点：GIN 索引建在 `to_tsvector('simple', document)` 上，但 `_sparse_search` 查的是 `metadata->>'search_text_sparse'`，**表达式不同、索引不生效**，等于全表扫描。

3.2.6 语义分块的断点是怎么找的？threshold 是多少？如果整段相邻句相似度都高于 threshold，会怎样？（⭐⭐）

前后两句语义是否有语义断崖的情况，有的话就断开。段落结尾也会断开。当超过thredshold时，也会找到上一点端口。还真没想过。

> 📝 **点评**：断崖对。但三处要补：① 是先按 `## ` **标题**切 section，不是"段落结尾"；② threshold=0.5 没答出来；③ 全段都高于阈值时没有断点，靠 `max_chars=800` 硬切兜底。

3.2.7 关键词加权对**中文查询**生效吗？为什么？（⭐⭐⭐）

生效，虽然关键词里面都是英文，但是中文会被捕捉翻译

> 📝 **点评**：❌ 错。**没有"翻译"这回事**——`_extract_tech_terms` 只有 4 组英文正则（PascalCase/连字符/全大写/点分），**根本没有 CJK 正则**，所以中文查询的关键词加权**不生效**，基本空转。

3.2.8 邻接 chunk 召回是干嘛的？没有它会出什么具体问题？（⭐⭐）

因为相邻chunk内的信息有更大概率有相关的地方，邻接chunk召回有效保证信息的完整度。没有它召回信息会出现不完整

> 📝 **点评**：✅ 准确。补一句：用 prev/next chunk_id 补回被语义分块切碎的知识点，防止断章取义。

3.2.9 如果 qwen3-rerank 超时或返回非 200，你的检索整体会怎样？（⭐⭐）

会下降，从rerank下降到字典信息

> 📝 **点评**：方向对但不准确。不是"下降到字典"——`_rerank` 返回空就**跳过 rerank**，直接用 RRF 排序结果往下走，靠 `score≥0.6` 过滤。检索不崩，只是少了精排。

### 3.3 意图识别（Router）

3.3.1 你的意图识别是"规则 + LLM 两阶段"吗？**规则那一阶段实际会不会被触发？confidence 是谁给的？**（⭐⭐⭐）

规则那一段是捕捉关键字，实际会被触发。confidence是LLM自己根据相似度自己给tag打分

> 📝 **点评**：❌ **两处都反了**。规则版 `route_intent_sync` 硬编码 `confidence=0.5`，升级条件是 `<0.8`，所以只要模型可用**每次必调 LLM**，规则是死代码（只在 LLM 抛异常时兜底）。confidence 是**硬编码**的（LLM 版 0.9 / 规则版 0.5），模型根本没输出。

3.3.2 路由是单轮还是多轮的？看不看对话历史？这会导致什么具体问题？（⭐⭐）

路由是可以多轮的，如果信息不足，会返回给用户补充信息。第一次不看对话历史。上下文信息隔离

> 📝 **点评**：🟡 混淆了两个概念。**路由（router）本身是单轮**——`_get_last_user_message` 只看最后一条，不读历史。你说的"信息不足返回补充"是 **planner/clarify 的追问**，不是路由。路由和追问是两码事。

3.3.3 6 个场景是互斥的。如果用户一句话同时是"调研"又是"代码"，会怎么判？你的方案有什么局限？（⭐⭐⭐）

这个时候就会有冲突，会返回给用户再确认。确实有局限，但是这种情况不多，因为一般这两问题是有先后次序的

> 📝 **点评**：❌ 代码**不会"返回给用户确认"**。6 场景互斥硬判，`_clean_scene` 取第一个命中的场景代码就返回了，没有冲突检测、没有确认回环。你答的是"理想该怎么做"，不是"代码实际怎么做"。

3.3.4 `confidence` 为什么是硬编码 0.9 / 0.5，而不是模型输出？拿不到真置信度对你后续流程有什么影响？（⭐⭐）

因为confidence给大模型来说很容易产生幻觉，它自己的输出和验证在没有干预的情况下很容易自洽，硬编码在牺牲一定的灵活性提升了速度和一定的鲁棒性。拿不到真置信度在后续check和reflect质量检测都会有一定影响，但是也不会影响特别大，

> 📝 **点评**：🟡 有思辨，但这是**把"没实现"合理化成"有意为之"**。真原因是代码直接写死了 0.9/0.5，没让模型输出置信度。"硬编码提升鲁棒性"站不住——真鲁棒性应该是模型输出 `confidence + reason`，低置信度时追问。面试官会追问"那你现在拿不到真置信度，怎么判断要不要追问？"——答案是：现在确实没做这个。

### 3.4 Planner 与任务契约

3.4.1 完整度 `completeness` 到底是**代码算的**还是 **LLM 估的**？`calculate_completeness` 这个函数在代码里被谁调用？（⭐⭐⭐）

这个是代码算的，多个信息通过权重相加满足大于0.8就算完整。这个函数被execute节点调用

> 📝 **点评**：❌ **全错**。`completeness` 是 **LLM 直接输出的**（`plan.get("completeness")`），`is_complete` 也是 LLM 拍的，驱动路由。`calculate_completeness`（权重相加那个）只在 `_generate_fallback_questions` 里被调用，用于 LLM 没给追问时兜底。而且阈值不是 0.8——是 LLM 自己估的任意值。

3.4.2 你项目里有两套权重——维度权重（purpose 0.25…）和契约 confidence 加权（goal 30%…）。**它们是同一套还是两套？分别在哪个环节生效？**（⭐⭐⭐）

这个是两套权重，前者是？？？

> 📝 **点评**：两套对。各自在哪生效：维度权重（purpose 0.25…）→ 拼 `dims_desc` 注入 prompt + `calculate_completeness` 兜底；契约 confidence 加权（goal 30%…）→ 写在 `PLANNER_SYSTEM_PROMPT` 里让 LLM 估 `contract.confidence`。

3.4.3 任务契约里为什么说 scope.out 比 scope.in 更重要？空 scope.out 会触发什么？（⭐⭐）

？？？

3.4.4 JSON 解析的三层降级分别是什么？第 4 层兜底返回什么、为什么故意让 `is_complete=False`？（⭐⭐）

json解析降级输出，因为会出现输出过长，处理的时候内存不足，需要逐渐降级处理，用更粗糙的处理，第四层直接返回json原文。。。

> 📝 **点评**：❌ **全错，方向就偏了**。三层降级是**解析手段**的降级，跟"内存不足"无关：① `json.loads` 直接 parse；② 抠 ```` ```json ```` 代码块；③ 正则 `\{.*\}` 抠第一个 `{...}`。第 4 层兜底返回 `{"is_complete": False, "completeness": 0.3, ...}`，**故意让 `is_complete=False` 把流程推向追问**，不是"返回 json 原文"。

3.4.5 `_fallback_plan` 在**什么条件下**触发？它用什么信号判断完整度？和"LLM 回复了但 JSON 解析失败"是同一条路径吗？（⭐⭐⭐）

？？？

3.4.6 多轮里新提取的维度怎么和旧的合并？什么条件下才会覆盖旧值？（⭐⭐）

在置信度更高的时候会覆盖旧值。

> 📝 **点评**：✅ 准确。补一句：保守合并，防止低置信度的新提取冲掉已确认信息。

### 3.5 ReAct / 工具（skills & tools）

3.5.1 skill 和 tool 是什么关系？画一下它们和 `SKILL_TOOL_MAP`、`_SKILL_SYSTEM_PROMPTS` 的对应。（⭐⭐）

每种skill是实现相应的目的，在里面会调用不同的tool来实现目的。。。

> 📝 **点评**：🟡 太浅，没答到"对应关系"。要说出两张表：`_SKILL_SYSTEM_PROMPTS` 管人设（这个 skill 是谁、擅长什么），`SKILL_TOOL_MAP` 管工具白名单（这个 skill 能调哪些 tool）。skill = 人设 + 工具白名单，tool = 单个 `@tool` 函数，多对多。

3.5.2 execute 节点里 **ReAct agent 到底建了几次？** skill 类自己的 `execute()` 方法在什么情况下才会被调用？（⭐⭐⭐）

执行节点内react

> 📝 **点评**：❌ 等于没答。核心是：ReAct **建了两次**——`execute_node` 直接 `create_react_agent`（主路径），skill 类自己的 `execute()` 内部又建一次；而 `skill_instance.execute()` 只在主路径没产出文本时才被 `_execute_legacy_skill` 调用，所以是**死代码**。

3.5.3 `bind_tools` 和 `create_react_agent` 各自干了什么？`parallel_tool_calls=True` 意味着什么？（⭐⭐）

？？？

3.5.4 为什么 3 个工具（python_exec / run_shell_preview / write_file）不分配给任何 skill？这是能力问题还是定位问题？（⭐⭐）

这个是后续版本要用的，现在还在考虑具体拓展哪些能力，所以暂时没有使用这些tool

> 📝 **点评**：❌ 错。是**定位问题**——阿福是"协作引导者"不是"执行者"，`python_exec`（跑代码）、`run_shell_preview`（跑 shell）、`write_file`（写文件）这三个"动手"工具**故意不暴露**，`tools/__init__.py` 注释写得清清楚楚，不是"预留待用"。

3.5.5 你的 ReAct 循环有没有轮数上限？靠什么兜底防止 LLM 无限调工具打转？（⭐⭐⭐）

有循环次数的count在check和reflect处

> 📝 **点评**：❌ **混淆了两套限制**。check/reflect 的 count 是**质检重试**（`MAX_CHECKPOINT_RETRIES=1`、`MAX_REFLECTION_RETRIES=2`），管的是"语义对齐/质量不过关重跑 execute"。ReAct 的 tool 轮数上限是 `max_tool_rounds=10`，但**没传进 `create_react_agent`**，实际靠 LangGraph 默认 recursion limit 兜底。

3.5.6 `create_react_agent` 内部的 token 能冒泡到外层 `astream_events` 吗？如果不能，你是怎么处理流式的？（⭐⭐⭐）

？？？


### 3.6 工程规范顾问（Engineering Advisor）

3.6.1 三级输出模式是哪三级？分级依据是"重要程度"还是"风险等级"？（⭐⭐）

？？？

3.6.2 "降级匹配"为什么叫降级？**它现在是降级还是唯一在跑的路径？** 主路径（RAG 检索）接上了吗？（⭐⭐⭐）

？？？

3.6.3 一次命中多个场景时，最终 mode 怎么定？取平均还是取最高？（⭐⭐）

？？？

### 3.7 流式输出（SSE）

3.7.1 你的流式是**节点级**还是 **token 级**？execute 节点内部的 token 为什么看不到？（⭐⭐⭐）

3.7.2 客户端中途断连，SSE 连接和后台 graph 执行怎么处理？（⭐⭐）

### 3.8 可靠性与测试

3.8.1 LLM Agent 项目的测试策略，和 CRUD 项目最根本的区别是什么？你"测确定性、监控非确定性"具体怎么落地？（⭐⭐）

这个还在学习中，目前只对这个agent进行了冒烟测试

> 📝 **点评**：诚实，但暴露了测试这块是空白。这道是必考题，至少要能说出核心思路：**"测确定性部分（维度合并/JSON 解析/完整度计算/降级逻辑），监控非确定性部分（token 用量/延迟/失败率/语义漂移）"**。你说"只做了冒烟测试"等于主动送分。

3.8.2 25 个测试分别覆盖了什么？哪些是你故意不测的，为什么？（⭐⭐）

？？？不知道测了什么

> 📝 **点评**：⚠️ **危险，这是你自己的项目**。25 个测试：`test_graph.py` 21 个（维度合并/完整度/追问生成/JSON 解析/降级）+ `test_session_store.py` 5 个（Session CRUD/消息读写/级联删除/反馈统计）。面试官一听"不知道测了什么"直接判定你没真正参与测试工作。

### 3.9 综合追命连环问

3.9.1 用户说"帮我做一个论坛"。从 router 到 reflect 完整走一遍，**每个节点做什么、哪一步最可能出问题**？（⭐⭐⭐）

router捕捉关键词帮我做，然后对询问enrich，再在rag库里面查相关信息，之后交给planner，进行安排，需要再明确clarify就再问询补充信息。直到通过后看是否需要多agent工作，需要交代任务，不需要就继续向下执行checkpoint，看意图是否偏离，有问题就纠正再check，如果超过limitcuont就回给用户补充信息，没问题就执行，最后check和reflect查看质量。这里最容易出问题就是意图识别planner处

> 📝 **点评**：🟡 主体流程对。三处要修：① router 不是"捕捉关键词"（是 LLM 文本分类）；② "超过 limitcount 回给用户补充信息"**不对**——checkpoint/reflect 超限是回 execute 重试，不是回给用户；③ 最容易出问题的不是 planner，是 **execute 的 ReAct 语义漂移**（跑偏成"写规划文档"而非执行，见 `docs/semantic-drift.md`）。

3.9.2 如果 embedding 服务整体挂了，从头到尾哪些环节会受影响？完整的降级链是怎么串起来的？（⭐⭐⭐）

？？？

3.9.3 一个"个人助手"，你上了 PGVector + 知识图谱 + 三层上下文 + 多 Agent Panel + ReAct + checkpoint/reflect 双重质量检查。**是不是过度设计？** 哪个是你觉得可砍的？（⭐⭐⭐）

我在学习到新知识会尝试使用并且学习实现，让这个技术对于我是个灰盒，因此现在又这么多技术，我会觉得多agent panel是过度设计了，这个会删掉

> 📝 **点评**：✅ 有自我审视，这是加分项。补一句更完整：多 Agent Panel 只在"明确要求对比"时才触发、频次低，收益可用"一个 LLM 分三段输出"近似，所以可砍。

3.9.4 如果现在让你把这个项目砍掉一半复杂度，你会砍哪一半？为什么？（⭐⭐⭐）

砍在砍掉严苛的意图识别，因为用户的模糊询问和上下文不一致很容易造成前后矛盾，意图识别放宽后，把选项返回给用户会更快更节省

> 📝 **点评**：🟡 有想法，但方向偏。意图识别其实已经够轻了（就一次 LLM 分类），砍它省不了多少复杂度。真正该砍的是**三套记忆的重叠**（ContextEngine L3 事实 / SessionMemory 摘要 / UserProfile 画像，功能重叠、重复抽取）和**知识图谱的 Claim 层**（存了但没被消费）。

3.9.5 你项目里最薄弱的、最不愿被问到的那个点是什么？（⭐⭐⭐）

执行节点吧，因为里面经常出问题

> 📝 **点评**：🟡 诚实但太模糊。面试官会追"执行节点具体哪出问题？"——你要能说出：ReAct 语义漂移（跑偏）、token 不冒泡（子图）、`max_tool_rounds` 没接上。能具体到 bug 才算答到位。

---

## 阶段 4：系统设计与场景题

4.1 现在 100 个用户**并发**使用，你的哪些地方会先出问题？（内存字典、PGVector 连接、SSE 连接数……）分别怎么改？（⭐⭐⭐）

？？？

4.2 知识库从 20 个 md 涨到 20 万个，索引和检索哪里先成为瓶颈？稀疏检索、rerank、embedding 各自怎么扩？（⭐⭐⭐）

？？？

4.3 你的记忆系统说"跨会话"，但 L3 检索又按 `session_id` 过滤。如果用户开新会话却想引用上周的决策，你现在怎么做？要改的话怎么改？（⭐⭐）

我这里采用了下载上次对话的功能，需要用户保存了上次生成的md文档，下次对话将这个文档发出来即可

> 📝 **点评**：🟡 你答的是**现状的 workaround**，不是题目问的"要改怎么改"。而且"下载 md 再上传"本身就暴露了跨会话能力弱——理想是给 L3 加跨会话兜底，或引入"项目/关联会话"概念让记忆按项目聚合，而不是让用户手动搬运文档。

4.4 上下文窗口已经到 1M 了，你的三层上下文 + RAG 会不会反而浪费？什么时候该全塞进去、什么时候该走检索？（⭐⭐⭐）

？？？

4.5 如果要给 Alfred 加"定时任务/主动推送"，你会怎么设计？它和现在的请求-响应模型冲突在哪？（⭐⭐）

？？？

---

## 阶段 5：反问环节（给你用，也考你）

> 面试官说"你有什么想问我的"时，问这些比问"加班吗"更显水平。

5.1 团队现在 Agent 项目里，检索质量和成本之间的平衡是怎么做的？有专门的评测集吗？

5.2 你们对 LLM 输出的幻觉/质量，是靠框架层约束还是靠评测反馈闭环？

5.3 这个岗位入职前 3 个月，最希望我独立扛下哪块？

5.4 团队里 Agent 的"可观测性"（token 用量、延迟、失败率）是怎么做的？

---

## 使用建议

1. **先盲答**：照着阶段 3 的 ⭐⭐⭐ 题，口头或手写，逼自己用代码级细节回答，不要翻代码。
2. **再回查**：答完对照源码核实，重点看 3.1.5、3.2.4、3.2.5、3.3.1、3.4.1、3.4.2、3.5.2 这几道——它们都在考"文档/注释和真实代码是否一致"。
3. **练连环**：每道 ⭐⭐⭐ 题都假设面试官会追一句"为什么 / 那怎么办"，提前备好下一层答案。
4. **诚实比炫技重要**：3.9.3 / 3.9.4 / 3.9.5 没有标准答案，面试官在考你有没有自我审视能力，不是考你会不会编。
