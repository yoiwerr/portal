# Q3.1.4 话题切换中间地带真的调了 LLM 精检吗

> **状态**：？？？ | **考点**：注释写了、代码没实现

## 正确答案

**没有。** 代码里重叠率落在中间地带（`0 < overlap < 0.3`）时，注释写了"未来可加 LLM 精检"，还留了一段示例 prompt，但实际逻辑是**保守处理、不重置摘要**（直接 `return False`）。

## 关键解析

`_detect_topic_switch`（`context_engine.py:269`）实际是**单级检测**：
- 零重叠 → 判切换（重置 L2 摘要）
- 重叠率 ≥ 0.3 → 判不切换
- 中间地带（0~0.3）→ **不重置**（注释里说"未来可加 LLM 精检"，但没实现）

所以所谓"keyword 重叠 + LLM 精检"的"两级检测"，实际只有 keyword 快检这一级在跑。

## 面试一句话

> 实际没实现。中间地带注释写了"未来可加 LLM 精检"，但代码是保守处理、不重置摘要。这个 LLM 精检是个没落地的 TODO——文档写的"两级检测"实际只有一级。
