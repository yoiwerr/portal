# Q3.3.3 6 场景互斥硬判的局限

> **状态**：❌ 答错 | **考点**：代码不会"返回用户确认"

## 你的答案

> 这个时候就会有冲突，会返回给用户再确认。确实有局限，但是这种情况不多

## 正确答案

**代码不会"返回给用户确认"。** 6 场景是互斥硬判：`_clean_scene`（`router.py:137`）取第一个命中的场景代码就返回了，没有冲突检测、没有确认回环、没有多标签、没有优先级。

## 关键解析

你答的是"理想该怎么做"，不是"代码实际怎么做"。实际行为：一句话既是"调研"又是"代码"时，`_clean_scene` 遍历 `valid` 集合 + keyword mapping，第一个命中就 return，剩下的意图直接丢弃。

## 面试一句话

> 现在互斥硬判，`_clean_scene` 取第一个命中的场景就返回，没有多标签、没有冲突检测，不会回给用户确认。更合理的是允许多标签或输出置信度排序，但我现在没做。
