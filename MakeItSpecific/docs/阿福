目前我们对“阿福”的定位，已经从“自动执行型 Agent”逐渐收敛为一个更清晰的方向：

## 核心定位

**阿福不是替用户和其他 Agent 做所有事情，而是作为人与智能体之间的协作管家。**

它的核心价值不是“直接改代码”或“自动完成一切”，而是：

> **帮助用户想清楚任务、选择合适能力、规范协作过程、保留项目记忆。**

原项目强调从聊天助手走向执行副驾驶，而现在的阿福更进一步强调：执行之前先确保方向正确、边界清晰、过程可靠。

## 四项核心能力

### 1. 想清楚：任务澄清

当用户的目标、限制或验收标准不明确时，阿福不会让其他 Agent 直接行动，而是判断信息是否充足，只追问那些会真正影响结果的问题。

最终形成一份简洁的“任务契约”，包括：

* 最终目标
* 工作范围
* 技术或业务约束
* 验收标准
* 风险边界
* 允许执行的权限
* 期望交付结果

阿福不是单纯“优化提示词”，而是把用户模糊的想法整理成其他 Agent 可以可靠执行的任务说明。

### 2. 选对人：能力建议与协作路由

阿福不需要证明自己什么都会做，而是知道：

> 什么任务适合自己辅助，什么任务更适合交给其他模型、Agent、Skill 或工具。

例如：

* 视觉稿可以建议使用 ChatGPT 图像能力；
* 长文档或项目资料分析可以考虑 Claude；
* 特定任务可以建议使用相应 Skill；
* 涉及风险或关键决策时，也可以建议由人处理。

推荐不会做得过度精确，也不会声称“某模型一定最好”，而是采用轻量表达：

> 这个环节可以考虑使用某项能力，因为它更适合当前任务。需要的话，阿福可以帮你准备交接材料。

推荐的最小结构是：

**建议对象 + 适用原因 + 下一步帮助**

阿福只在其他能力能明显提升结果、降低风险或减少返工时提出建议，避免每个任务都推荐工具。

## 3. 看住过程：工程规范与安全辅助

阿福的辅助不只是聊天，还应帮助用户在项目过程中符合基本工程范式，例如：

* 先确认目标与框架
* 控制项目范围
* 建立合理的 Git 提交节奏
* 保留关键决策
* 提醒测试、安全和验收
* 检查不同阶段的产物是否一致
* 在高风险操作前要求确认

但阿福不能成为一个不断说教的检查清单。它应该根据项目规模动态调整规范，并尽可能把复杂流程压缩成少量关键选择。

它的作用不是增加流程，而是减少返工和失控。

## 4. 记住结果：上下文继承

每次工作结束后，阿福生成一份简短的“项目交接卡”或“上下文快照”，用于下一次快速恢复状态。

建议包含：

* 当前目标
* 已完成事项
* 已确认决策
* 未解决问题
* 下一步行动
* 关键文件或资料
* 新发现的用户偏好
* 下次需要验证的信息

这比保存完整聊天记录更实用，也能避免产生大量重复文档。

## 三层记忆结构

### 工作记忆

保存当前任务中的临时信息：

* 当前目标
* 当前阶段
* 用户刚刚确认的内容
* 待解决问题
* 正在比较的方案

任务结束后可以清理或压缩。

### 用户与项目心智模型

保存相对长期的原子信息：

* 用户技术栈
* 工作习惯
* 沟通偏好
* 项目框架
* 已确认决策
* 安全要求
* 常用工具
* 不希望被频繁追问等偏好

这些信息不应只是随意总结，而应尽量具有来源、更新时间和可信度，避免错误信息永久进入记忆。原项目中关于项目心智模型、用户偏好和知识衰减的设计可以继续保留。

### 能力知识库

用于保存外部模型、Agent、Skill 和工具的能力信息。

它属于公共知识，不属于某个具体用户。

## 能力卡片设计

能力知识库不需要存放大量完整产品文档。比赛版本可以主要保存经过压缩的“能力卡片”。

每个能力卡片作为一个完整 chunk，不再让语义分割器继续拆分。

建议字段包括：

* 名称
* 类型：模型、Skill、工具或 Agent
* 一句话能力
* 适用场景
* 明显限制
* 推荐表达
* 相关能力
* 信息来源
* 更新时间

例如：

> **ChatGPT 图像生成**
> 适合宣传海报、概念图、视觉草图和图片修改。
> 不建议输入敏感项目资料，复杂文字可能需要后续调整。
> 推荐表达：“这个环节可以考虑使用 ChatGPT 生成视觉稿，我可以先帮你整理画面和提示词。”

能力卡片可以直接存放在现有 RAG 中，但最好通过元数据标记为 `capability_card`，方便定向检索。

## RAG 的边界

RAG 主要存：

* 能力卡片
* 精简后的最佳实践
* 必要的功能说明
* 更新后的能力结论

不必存：

* 每个平台的全部官方文档
* 大量重复更新日志
* 每个用户的技术栈和偏好
* 当前任务的临时上下文

用户技术栈、项目情况和个人偏好应放在工作记忆或心智模型中，而不是混入公共能力库。

整体逻辑是：

> **工作记忆告诉阿福现在要做什么；
> 心智模型告诉阿福用户和项目是什么；
> 能力知识库告诉阿福谁可能适合做。**

## 推荐功能的交互原则

推荐阶段不需要频繁确认用户是否安装某个 Skill。

例如，阿福可以先说：

> 这类任务可以考虑使用 Claude，需要的话我可以帮你整理交接说明。

只有用户决定采用 Claude，并且后续步骤确实依赖某个 Skill 时，才需要核对是否安装。

原则是：

> **推荐阶段确认大方向，执行阶段才核对具体条件。**

这样可以避免阿福为了“推荐准确”而连续追问套餐、版本、权限和安装情况。

## 多 Agent 的使用方式

多 Agent 不建议仅通过不同温度生成多份相似答案，而应设置不同决策立场，例如：

* 实用派：优先速度和最小实现
* 稳健派：优先安全和维护成本
* 创新派：优先差异化与创意
* 阿福：整理共识、分歧和待用户决定事项

最终输出不应是三篇长文，而应是结构化对比：

* 共识是什么
* 分歧在哪里
* 每个方案的代价是什么
* 用户真正需要决定什么
* 阿福倾向于哪种方案以及原因

## 代码与 Git 接入

阿福可以读取代码和项目文件，但初期不必赋予修改权限。

比赛版本更适合采用：

> **可读、可理解、可辅助，但默认不可写。**

可以逐步演进：

1. 用户选择本地项目文件，只读分析；
2. 接入只读 Git 仓库，理解结构、提交历史和决策；
3. 将确认后的任务交给执行 Agent；
4. 高风险操作始终需要额外授权。

代码和 Git 接入的目的不是展示“阿福会写代码”，而是让它能够真正理解项目背景、判断建议是否适合，并形成可靠的心智模型。

## 最终产品叙事

阿福的完整协作流程可以总结为：

> **想清楚——选对人——看住过程——记住结果**

更具传播力的表达是：

> **普通 Agent 总想证明自己什么都能做；阿福首先判断，谁最适合做。**

或者：

> **其他 Agent 负责行动，阿福负责确保行动是对的。**

阿福最终不是一个模型推荐器，也不是一个提示词生成器，而是：

> **守在用户与智能体之间的 AI 协作管家。它帮助用户明确目标、适时推荐能力、规范工作过程，并让每次项目协作都能从上次结束的地方继续。**
